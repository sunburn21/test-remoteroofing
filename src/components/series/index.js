// importing libraries
import React from 'react';

// importing required components
import Items from '../items';

// component acting as wrapper for the series list
export default ()=>{
    return(
        <Items type='series'/>
    )
}